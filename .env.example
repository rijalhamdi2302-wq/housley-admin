# Housley Admin — Environment Variables
# Copy to .env and fill in values.

# Backend API URL (no trailing slash)
# For local dev, leave empty (Vite proxy handles it)
# For production, set to your Render backend URL, e.g. https://housley-api.onrender.com
VITE_API_URL=
